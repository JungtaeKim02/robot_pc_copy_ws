# 이 파일은 robot PC 의 inha_interfaces/srv/ExecuteGraspSrv.srv 에 추가해야 할
# 필드를 안내하기 위한 패치 노트입니다 (실제 srv 파일 아님).
#
# 기존 ExecuteGraspSrv.srv 의 Request 부분 (--- 위) 에 아래 한 줄을 추가하세요.
# 다른 필드는 그대로 둡니다.
#
# ---------- 추가 ----------
string request_id                 # manager 에서 생성한 UUID. 빈 문자열이면
                                  # 노드 내부에서 새로 생성하여 사용한다.
# ---------- 추가 끝 ----------
#
# 결과적으로 ExecuteGraspSrv.srv 는 대략 다음 형태가 됩니다:
#
#   string arm_id
#   geometry_msgs/PoseArray grasp_candidates
#   string mode
#   string request_id              ← 신규
#   ---
#   bool success
#   string message
